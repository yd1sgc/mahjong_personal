# 麻雀スコアアプリ 設計・移行計画

## 目的

`main` の安定した四麻入力・記録を土台に、以下を安全に追加する。

- グループごとの対局・成績管理
- メンバーの複数グループ所属
- 公式ルールとカスタムルールのテンプレート管理
- 対局時点のルールに基づく、過去成績の不変な集計
- 親族麻雀のメンバー以外も含めた個人成績の記録

安定性と既存データ互換を最優先とし、オンラインDBはこの段階では変更しない。

## 確定した利用ルール

### 親族麻雀グループ

- `main` にある既存メンバー全員を、初期グループ **親族麻雀** に登録する。
- 新しい4人組は、初期グループ **麻雀部** として登録する。
- 親族麻雀を選んで開始した対局は、外部参加者がいても親族麻雀として保存する。
- 通常の親族麻雀成績には、対局時点で4人全員が親族麻雀の所属メンバーだった対局だけを表示する。
- 「外部参加者を含む」をオンにすると、親族麻雀として保存した全対局を表示する。
- 全体成績では、グループに関係なくすべての対局を表示する。

### ルールテンプレート

- 公式テンプレートは固定・編集不可とする。
- カスタムテンプレートは公式テンプレートを複製して作成する。
- カスタムテンプレートは削除せず、アーカイブする。
- アーカイブ済みテンプレートは新規対局で選べないが、過去対局の詳細では閲覧できる。
- 対局開始時のルール設定を完全なスナップショットとして対局に保存する。
- テンプレートを後から変更・アーカイブしても、過去の対局結果・成績は変えない。

### 対応範囲

最初の完成版は四麻専用とする。ルールに `player_count = 4` を持たせ、三麻は後続の独立した拡張として扱う。

カスタム画面で操作可能にするのは、実際に計算または進行へ反映する項目だけとする。未対応項目は設定選択肢として表示しない。

公式テンプレートの詳細画面では、将来対応の候補を読み取り専用で表示する。

- 対応済み: 実際に計算・進行に反映する。
- ルール説明: 公式ルールとして表示するが、アプリは自動判定しない。
- 将来対応予定: 将来の実装候補として表示する。

## データモデル

### 正本となる新しいモデル

| エンティティ | 主な項目 | 用途 |
| --- | --- | --- |
| `members` | `member_id`, `display_name`, `is_archived` | 個人を不変IDで管理する。表示名の重複を許容する。 |
| `groups` | `group_id`, `group_name`, `is_archived` | グループを不変IDで管理する。 |
| `group_members` | `group_id`, `member_id` | 現在の所属関係を管理する。 |
| `rule_templates` | `rule_id`, `name`, `kind`, `version`, `config_json`, `is_archived` | 公式・カスタムのルールテンプレートを管理する。 |
| `games` | `game_id`, `selected_group_id`, `rule_snapshot_json`, `rule_name_snapshot`, `rule_schema_version` | 対局時点のグループとルールを不変に残す。 |
| `game_participants` | `game_id`, `seat`, `member_id`, `display_name_snapshot`, `score`, `rank`, `was_group_member` | 対局参加者と、対局時点のグループ所属を残す。 |

### 互換性方針

- 既存の `games.p1_name` ～ `p4_name`、点数、順位の列は直ちに削除しない。
- 新形式の `game_participants` を追加し、新規対局から利用する。
- 旧対局は既存列から読めるようにし、必要に応じて安全なローカル移行で参加者行を補完する。
- `main` の既存対局には、当時の標準ルールのスナップショットを適用する。
- グループが未設定の旧対局は、全体成績にのみ表示する。親族麻雀へ自動分類する場合は、参加者が全員親族麻雀の初期メンバーであることを確認してから行う。

## ルールモデル

ルール設定の保存形式は一種類だけに固定する。画面・DB・計算で旧形式と新形式の分岐を持たない。旧データの変換は移行処理だけに閉じ込める。

### 最初に実装する設定

- プレイヤー数: 4（固定）
- 東風戦／半荘戦
- 初期持ち点
- 返し点
- ウマ
- オカ
- 丸め
- レート換算メモ
- 食いタン、後付け、赤牌
- 喰い替え
- 飛びの有無と終了基準
- 西入条件
- アガリやめ・テンパイやめ
- 親連荘条件
- 流局ノーテン罰符
- 途中流局（九種九牌、四風連打、四家立直、四槓散了）
- ダブロン・トリロン
- 役満複合・パオ（責任払い）
- チョンボの支払い方式

### 後続で扱う設定

- 流し満貫
- チップ、焼き鳥、罰符
- 三麻

これらは、点数計算・和了の合法性判定・追加精算を実装する段階で、設定UIとテストを同時に追加する。

## 状態管理

対局中の状態は `st.session_state` を唯一の正本とする。個別キーへの直接アクセスを減らし、状態操作は `game_logic.py` の関数へ集約する。

`GameStateContainer` と個別の `st.session_state` を二重に保持しない。対局下書きも同じ状態形式で保存・復元する。

## 機能仕分け

### `main` から再利用する

- 四麻の入力画面と基本的な対局記録
- 和了・流局・チョンボの入力フロー
- 親連荘、聴牌連荘、ノーテン罰符
- 西入・北入なしの基本的な終了判定
- 既存の成績表示と点数計算のテスト基盤

### 現ブランチから採用して作り直す

- ホーム画面からの画面遷移
- グループ選択と既定ルール
- 公式テンプレートとカスタムテンプレート
- 対局へのグループID・ルールスナップショット保存
- グループ別・ルール別の成績フィルター
- ローカル開発DBの分離

### 現ブランチで解消してから進める問題

- ルール設定の旧形式と新形式が混在している。
- ルール画面で選べる項目の多くが対局処理に反映されない。
- 計算・終了判定・流局・チョンボに固定値が残っている。
- `GameStateContainer` と `st.session_state` が二重化している。
- グループ所属を名前文字列で保存している。
- 表示用の `G01` 等が並び順依存で不変IDではない。
- ローカルDBとオンラインDBのスキーマ・同期対象が一致していない。

## 段階的な実装順

1. 現ブランチを凍結し、再構築用ブランチで作業する。
2. ローカルDB用のスキーマ移行と、旧データ読込互換を作る。
3. メンバー・親族麻雀グループ・所属管理をID中心へ移す。
4. 公式テンプレート、複製によるカスタムテンプレート、アーカイブを作る。
5. 対局開始時のルールスナップショットと参加者スナップショットを保存する。
6. 点数計算、結果のみ入力、詳細入力、終了判定をルールモデル経由へ移す。
7. 成績画面に全体／グループ／外部参加者を含む切替を追加する。
8. ローカルで移行・保存・集計・進行の自動テストを整備する。
9. ローカル版の安定後に、オンラインDB移行と複数端末対応を別計画で行う。

## 完了条件

- `main` の既存対局を読み込み、現在と同じ成績を表示できる。
- テンプレート編集後も、過去対局の成績が変わらない。
- 親族麻雀の通常表示では、外部参加者を含む対局が除外される。
- 「外部参加者を含む」で、その対局と外部参加者の成績を表示できる。
- 外部参加者の全体個人成績を確認できる。
- アーカイブ済みグループ・ルールでも過去対局の詳細を確認できる。
- 未対応のルールをカスタム設定として保存できない。
- ローカルDBでの自動テストが通る。
